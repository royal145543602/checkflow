services:
  - type: web
    name: checkflow
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: DB_PATH
        value: /data/checkin.db
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
